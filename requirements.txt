# ── Data acquisition ──────────────────────────────────────────────────────────
httpx[http2]          # async HTTP for post image downloads
tqdm                  # progress bars

# ── Dataset pipeline ──────────────────────────────────────────────────────────
pyarrow               # Parquet read/write (formula index + assembled dataset)
polars                # lazy streaming joins (formula index → posts)
datasets              # HuggingFace dataset export and Hub upload

# ── Formula rendering ─────────────────────────────────────────────────────────
matplotlib            # typeset PNG rendering via mathtext
sympy                 # LaTeX parsing for function graph detection
numpy                 # numeric evaluation for graph plots

# ── Retrieval — Stage 1 (GNN formula encoder) ─────────────────────────────────
# Install PyTorch and PyG BEFORE the packages below.  For PyTorch 2.1.2 + CUDA 12.1:
#   pip install torch==2.1.2 --index-url https://download.pytorch.org/whl/cu121
#   pip install torch_geometric
#   pip install torch_scatter torch_sparse torch_cluster \
#       -f https://data.pyg.org/whl/torch-2.1.2+cu121.html
pyyaml                # config loading
rank-bm25             # offline BM25 hard-negative mining
faiss-gpu             # GPU-accelerated nearest-neighbour index (A100s)
pytrec-eval           # official IR metrics (nDCG, MAP) via trec_eval bindings
